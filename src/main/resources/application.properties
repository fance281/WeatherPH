spring.application.name=java
server.port=8080
spring.thymeleaf.cache=false

# OpenWeatherMap API Configuration
app.openweather.api=https://api.openweathermap.org/data/2.5/weather
app.openweather.key=21ad053d07630286a343b2b1fb151c79

# ===============================================
# DATABASE CONFIGURATION (Updated for Render/Cloud)
# 
# NOTE: The values below are placeholders. On Render, you MUST 
# define the corresponding Environment Variables:
# SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
# ===============================================
# FIX: Added :jdbc: default to ensure the property is always valid. 
# The Render environment variable will override this at runtime.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# ===============================================
# APP BASE URL CONFIGURATION
# 
# This is the local default (http://localhost:8080). 
# You MUST override this on Render with your public URL (e.g., https://your-app.onrender.com).
# ===============================================
app.base-url=http://localhost:8080


# ===============================================
# EMAIL SERVER CONFIGURATION (for Gmail)
# ===============================================
# Ensure you are using the correct port for STARTTLS
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Your full Gmail address
spring.mail.username=enriquezlance25@gmail.com
# IMPORTANT: Use the 16-digit App Password generated from your Google Account
spring.mail.password=pmmz rspb opqo eecn

# Required properties for connecting to Gmail's SMTP server
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
